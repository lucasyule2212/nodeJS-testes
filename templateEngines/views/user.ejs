<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Usuário</title>
  </head>
  <body>
    <%- include('./partials/navbar.ejs')%> <!--Renderizaçao do navbar via Partials-->

    <!-- A semantica do EJS permite escrever javascript puro em docs html, desde que esteja dentro das chaves semanticas-->

    <% for (let user of users) { %> <% if (user.id%2==0) { %>
    <h1><%=user.name%></h1>
    <h3><%=user.phone%></h3>
    <% } %> <% } %>
  </body>
</html>
